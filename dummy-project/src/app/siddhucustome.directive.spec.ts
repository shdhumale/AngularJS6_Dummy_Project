import { SiddhucustomeDirective } from './siddhucustome.directive';

describe('SiddhucustomeDirective', () => {
  it('should create an instance', () => {
    const directive = new SiddhucustomeDirective();
    expect(directive).toBeTruthy();
  });
});
